# --- Neurosity (only needed for LIVE mode) ---
NEUROSITY_EMAIL=
NEUROSITY_PASSWORD=
NEUROSITY_DEVICE_ID=

# --- Mistral (optional; only used locally, never on the public site) ---
# Free tier: no credit card, SMS verification only. Leave blank to run without any model.
MISTRAL_API_KEY=
MISTRAL_MODEL=mistral-small-latest

# --- Server ---
PORT=5273
